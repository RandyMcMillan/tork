[package]
name = "tork_rs"
version = "0.1.0"
edition = "2021"
description = "Rust FFI bindings for the Tork workflow engine"
license = "MIT"
build = "build.rs"

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
bindgen = "0.71"
